name: Deploy

# Temporarily disabled — network is closed
# To enable: uncomment the on: section and set DEPLOY_HOST, DEPLOY_USER, DEPLOY_SSH_KEY secrets

# on:
#   push:
#     branches: [main]
#     tags: ["v*"]

# Deploy disabled — network is closed
# on:
#   push:
#     branches: [main]
#     tags: ["v*"]
on: []

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -e
            cd /opt/max-hermes
            git fetch origin
            git checkout main
            git pull origin main
            source venv/bin/activate
            pip install -r requirements.txt
            sudo systemctl restart max-bridge
            sleep 3
            sudo systemctl status max-bridge --no-pager
